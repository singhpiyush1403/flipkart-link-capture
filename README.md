# flipkart-link-capture
A Java Selenium automation script to capture all hyperlinks from Flipkart's homepage. Uses ChromeDriver and implicit waits to ensure reliable element detection. Prints the total number of links and their visible text in the console.
